GEMINI_API_KEY=your_gemini_api_key_here

# Firebase config is hardcoded in lib/firebase.ts (it's safe to be public —
# Firebase security is enforced by Security Rules, not by hiding this
# config), so no Firebase env vars are needed here.
